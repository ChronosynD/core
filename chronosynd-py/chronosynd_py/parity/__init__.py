"""Parity tooling. The Python reference is the source of truth and emits
a JSON file of inputs and expected outputs that the Rust integration test
loads. The contract keeps the two implementations from drifting silently"""
